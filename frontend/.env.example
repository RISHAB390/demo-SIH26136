# frontend/.env.example
# Copy this file to frontend/.env for local development.
# In Docker production, VITE_API_URL is set via --build-arg. Do NOT commit .env.

# URL of the FastAPI backend (local dev only — Docker uses /api via nginx proxy)
VITE_API_URL=http://localhost:8000
